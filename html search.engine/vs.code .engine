<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Searchly - Simple Search Engine</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f4;
      color: #333;
    }

    .container {
      max-width: 800px;
      margin: auto;
      padding: 50px 20px;
      text-align: center;
    }

    .title {
      font-size: 3rem;
      margin-bottom: 40px;
    }

    .search-box {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-bottom: 40px;
      flex-wrap: wrap;
    }

    .search-box input {
      width: 60%;
      min-width: 240px;
      padding: 12px;
      font-size: 16px;
      border-radius: 4px;
      border: 1px solid #ccc;
    }

    .search-box button {
      padding: 12px 20px;
      font-size: 16px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .search-box button:hover {
      background-color: #0056b3;
    }

    .results {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .result-card {
      background: white;
      padding: 20px;
      border-radius: 6px;
      text-align: left;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    }

    .result-card a {
      font-size: 18px;
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }

    .result-card a:hover {
      text-decoration: underline;
    }

    .result-card p {
      margin-top: 8px;
      font-size: 14px;
      color: #555;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="title">Searchly 🔍</h1>
    
    <div class="search-box">
      <input type="text" placeholder="Search something..." />
      <button>Search</button>
    </div>

    <div class="results">
      <div class="result-card">
        <a href="#">Example Result Title</a>
        <p>This is a short description of the result. It's just placeholder text for now.</p>
      </div>

      <div class="result-card">
        <a href="#">Another Example Title</a>
        <p>Another short result description goes here. You can replace this later with real data.</p>
      </div>
    </div>
  </div>
</body>
</html>